<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- 🩶 Reusable text style for animal detail info -->
    <style name="AnimalInfoText">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:layout_marginBottom">6dp</item>
        <item name="android:fontFamily">sans-serif</item>
    </style>

    <!-- 💬 Rounded input fields for edit text boxes -->
    <style name="RoundedInput">
        <item name="android:background">@drawable/rounded_input_bg</item>
        <item name="android:padding">14dp</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">16sp</item>
        <item name="android:layout_marginBottom">12dp</item>
        <item name="android:fontFamily">sans-serif</item>
    </style>

    <!-- 🟩 Uniform green farm-themed buttons -->
    <style name="GreenButton">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:backgroundTint">@color/green</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:padding">12dp</item>
        <item name="android:layout_marginTop">10dp</item>
        <item name="android:layout_marginBottom">8dp</item>
        <item name="android:elevation">3dp</item>
        <item name="android:fontFamily">sans-serif</item>
    </style>

    <!-- 🧭 Card-like container for animal details and forms -->
    <style name="CardContainer">
        <item name="android:background">@drawable/rounded_card_bg</item>
        <item name="android:padding">16dp</item>
        <item name="android:layout_margin">10dp</item>
        <item name="android:elevation">4dp</item>
    </style>

</resources>
